python -m venv exenv
exenv\Scripts\activate


Run this command directly on the command line to install cuda
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

For notebook installation
python.exe -m pip install ipykernel